# =============================================================================
# Recrutva — CI Workflow
# =============================================================================
#
# Runs on:
#   - Pull requests targeting main
#   - Pushes to main
#
# Checks:
#   1. Install dependencies (npm ci)
#   2. TypeScript type checking
#   3. ESLint
#   4. Tests (email service, templates, notifications)
#   5. Next.js production build
#   6. Docker image build
#
# Secrets / Variables handled:
#   - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: Required at build time (public key)
#   - NEXT_PUBLIC_APP_URL: Required at build time (public URL)
#
# =============================================================================

name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# Cancel in-progress runs when new commits are pushed
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege permissions — only read repository contents
permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  # Lint and Test (fast feedback)
  # ---------------------------------------------------------------------------
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type checking
        run: npx tsc --noEmit

      - name: ESLint
        run: npm run lint

      - name: Run tests (email service, templates, notifications)
        run: npx tsx --tsconfig tsconfig.test.json scripts/phase4-email-test.ts

  # ---------------------------------------------------------------------------
  # Production Build (requires Clerk publishable key at build time)
  # ---------------------------------------------------------------------------
  build:
    name: Production Build
    needs: lint-and-test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Next.js production build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY || vars.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY || 'pk_test_placeholder' }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL || vars.NEXT_PUBLIC_APP_URL || 'http://localhost:3000' }}
          NODE_ENV: production
        run: npm run build

  # ---------------------------------------------------------------------------
  # Docker Build (after application build passes)
  # ---------------------------------------------------------------------------
  docker-build:
    name: Docker Build
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY || vars.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY || 'pk_test_placeholder' }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL || vars.NEXT_PUBLIC_APP_URL || 'http://localhost:3000' }}
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}" \
            --build-arg NEXT_PUBLIC_APP_URL="${NEXT_PUBLIC_APP_URL}" \
            -t recrutva:${{ github.sha }} \
            .
